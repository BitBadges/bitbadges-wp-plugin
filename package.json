{
  "name": "bitbadges-wp-plugin",
  "version": "1.0.0",
  "description": "WordPress plugin for BitBadges authentication",
  "scripts": {
    "build": "cross-env VERSION=$npm_package_version node scripts/build.js",
    "build:dev": "cross-env VERSION=$npm_package_version-dev node scripts/build.js"
  },
  "devDependencies": {
    "archiver": "^5.3.1",
    "cross-env": "^7.0.3"
  }
} 